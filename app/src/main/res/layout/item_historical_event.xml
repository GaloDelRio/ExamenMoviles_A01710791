<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:cardview="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    cardview:cardCornerRadius="12dp"
    cardview:cardElevation="6dp"
    android:background="?attr/selectableItemBackground"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

<LinearLayout
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:orientation="vertical"
android:padding="16dp">

<!-- Fecha -->
    <TextView
        android:id="@+id/tvDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="?attr/colorPrimary"
        android:text="Fecha: "
        android:contentDescription="@string/date_description" />

<!-- Descripción -->
<TextView
    android:id="@+id/tvDescription"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:textSize="14sp"
    android:textColor="@color/black"
    android:text="Descripción del evento"
    android:maxLines="2"
    android:ellipsize="end"
    android:contentDescription="@string/event_description" /> <!-- Accesibilidad -->

<!-- Categorías -->
<TextView
    android:id="@+id/tvCategories"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:textSize="12sp"
    android:text="Categoría 1, Categoría 2"
    android:contentDescription="@string/categories_description"
    android:ellipsize="end"
    android:singleLine="true" /> <!-- Mejor apariencia si las categorías son muchas -->
</LinearLayout>
    </androidx.cardview.widget.CardView>
